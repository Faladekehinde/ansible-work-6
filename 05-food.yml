---
- name: Deploy web server
  hosts: n2
  become: yes
  
  tasks:
  - name: Install Git
    apt:
      name: git
      state: present

  - name: Host code
    git:
      repo: https://github.com/CeeyIT-Solutions/food3.git
      dest: /var/www/html
      update: yes
      force: yes 
...           
